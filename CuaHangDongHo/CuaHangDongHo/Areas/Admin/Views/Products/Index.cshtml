@model IEnumerable<CuaHangDongHo.Models.Product>

@{
    string title = "Danh sách sản phẩm";
    ViewBag.Title = title;
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <div class="row">
                    <div class="col-6">
                        <h3 class="card-title">Danh sách Sản phẩm</h3>
                    </div>
                    <div class="col-6">
                        @Html.ActionLink("Thêm mới", "Create", null, new { @class = "btn btn-success float-right" })
                    </div>

                    @{
                        if (!String.IsNullOrEmpty(ViewBag.ErrMsg))
                        {
                            <div class="alert alert-danger col-12 mt-3" role="alert">
                                @ViewBag.ErrMsg
                            </div>
                        }
                        else if (!String.IsNullOrEmpty(ViewBag.SuccessMsg))
                        {
                            <div class="alert alert-success col-12 mt-3" role="alert">
                                @ViewBag.SuccessMsg
                            </div>
                        }
                    }
                </div>
            </div>

            <!-- /.card-header -->
            <div class="card-body">
                <div id="example1_wrapper" class="dataTables_wrapper dt-bootstrap4">
                    <div class="col-sm-12">
                        <table id="tblProduct" class="table table-bordered table-striped dataTable dtr-inline" role="grid" aria-describedby="example1_info">
                            <thead>
                                <tr role="row">
                                    <th>
                                        STT.
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.Name)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.Img)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.Number)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.PriceVND)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.PriceSale)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.Status)
                                    </th>
                                    <th style="width: 13%">
                                    </th>
                                </tr>
                            </thead>
                            <tbody>
                                @{
                                    int stt = 1;
                                }
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>
                                            @stt
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.Name)
                                        </td>
                                        <td>
                                            <img src="@Url.Content("~/Content/img/products/" + item.Img)" height="150" />
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.Number)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.PriceVND)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.PriceSaleVND)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.StatusProduct)
                                        </td>
                                        <td>
                                            @Html.ActionLink("Xem", "Details", new { id = item.Id }) |
                                            @Html.ActionLink("Sửa", "Edit", new { id = item.Id }) |
                                            @Html.ActionLink("Xóa", "Delete", new { id = item.Id })
                                        </td>
                                    </tr>
                                    stt++;
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
        <!-- /.card-body -->
    </div>

</div>


@section BeforeCss {
    <link href="~/admin/css/Product.css" rel="stylesheet" />
}

@section AfterJs {
    <script src="~/Scripts/admin/products-js.js"></script>
}
